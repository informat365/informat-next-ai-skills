{
	"type": "function",
	"function": {
		"name": "_wb_complete_task",
		"description": "Complete (approve) a workflow task assigned to the current user in the workbench, optionally with comment and form data. No moduleId required.",
		"parameters": {
			"type": "object",
			"properties": {
				"taskId": {
					"type": "string",
					"description": "The ID of the task to complete."
				},
				"comment": {
					"type": "string",
					"description": "Approval comment or note."
				},
				"vars": {
					"type": "object",
					"description": "Process variables to set when completing the task."
				},
				"record": {
					"type": "object",
					"description": "Form data to save when completing the task."
				}
			},
			"required": ["taskId"]
		}
	}
}
