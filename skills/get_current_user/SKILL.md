# _get_current_user

## 技能ID
get_current_user

## 描述
获取当前用户，返回执行这次操作的用户。比如提示词里包括`我的`，就需要调用此方法

## 工具调用

### 思考
调用_get_current_user工具

### 工具名称
_get_current_user

### 参数
{
  "type": "object",
  "properties": {}
}
